# Plugins

Put third-party plugin DLLs here (built against `AgentRuntime.Plugins.Sdk`). They're loaded at
startup and appear in the workspace "Add connection" list. Plugins run inside the server process,
so only install ones you trust. See `docs/plugins.md`.
